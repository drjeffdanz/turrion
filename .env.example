# Postgres (matches docker-compose.yml)
DATABASE_URL=postgresql://turrion:turrion@localhost:5432/turrion

# Redis (Phase 1 queue; unused in the very first cut)
REDIS_URL=redis://localhost:6379/0

# Entity-reference causal window (seconds)
CAUSAL_ENTITY_WINDOW_SECONDS=900

# Phase 1: Ask Martus
# ANTHROPIC_API_KEY=sk-ant-...
